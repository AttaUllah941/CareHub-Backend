const PaymentGateway = {
  JAZZCASH: 'JAZZCASH',
  EASYPAISA: 'EASYPAISA',
};

const PAYMENT_GATEWAY_VALUES = Object.values(PaymentGateway);

module.exports = { PaymentGateway, PAYMENT_GATEWAY_VALUES };
