const ReviewStatus = {
  PUBLISHED: 'PUBLISHED',
  HIDDEN: 'HIDDEN',
  FLAGGED: 'FLAGGED',
};

const REVIEW_STATUS_VALUES = Object.values(ReviewStatus);

module.exports = { ReviewStatus, REVIEW_STATUS_VALUES };
